<%= form_for @candidate do |f| %>
  <%= f.text_field :name %>

  <%= hidden_field_tag "candidate[language]", "" %>
  <%= select_tag "candidate[language][en]", options_for_select([['Basic', 1], ['Good', 2], ['Excelent', 3]]) %>

  <%= f.submit %>
<% end %>
